export const AUTH_PATTERNS = {
	LOGIN: 'auth.login',
	REGISTER: 'auth.register',
	LOGOUT: 'auth.logout',
	OAUTH: 'auth.oauth',
}

export const PROVIDERS_PATTERNS = {
	GET_BY_PROVIDER: 'providers.getByProvider'
}